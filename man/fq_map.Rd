% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{fq_map}
\alias{fq_map}
\title{Redraw a scored section's severity map after a run}
\usage{
fq_map(run, slide_id, section = NULL)
}
\arguments{
\item{run}{An \link{fq_results} from \code{\link[=fq_run]{fq_run()}}.}

\item{slide_id}{A \code{slide_id} from \code{run@scores} (or \code{run@paths}).}

\item{section}{A section label such as \code{"A"} or \code{"B"}, as it appears in the
\code{section} column of \code{run@scores}. \code{NULL} returns a named list of maps, one
per section of the slide.}
}
\value{
An \link{fq_field}, or a named list of \code{fq_field}s when \code{section} is
\code{NULL}.
}
\description{
\code{\link[=fq_run]{fq_run()}} keeps the scores and the fit, not the sections themselves, so a
row in the scores table is not directly tied to its pixels. \code{fq_map()} closes
that gap for post-hoc review: it replays \code{\link[=fq_read]{fq_read()}} and \code{\link[=fq_split]{fq_split()}} with
the recipe the run recorded, then renders the section through the run's fit,
returning a plottable \link{fq_field} in one call. Because the read-and-split path
is deterministic, nothing is cached between runs; the map is recomputed on
demand from the run's \verb{@params} and \verb{@paths}.
}
\details{
\code{run[[slide_id, section]]} is shorthand for \code{fq_map(run, slide_id, section)},
and \code{run[[slide_id]]} for \code{fq_map(run, slide_id)}.
}
\seealso{
\code{\link[=fq_run]{fq_run()}}, \code{\link[=fq_render]{fq_render()}}
}
