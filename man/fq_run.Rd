% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{fq_run}
\alias{fq_run}
\title{Score a folder of slides against one analyzer}
\usage{
fq_run(
  manifest,
  analyzer,
  n = 2,
  close_um = 50,
  min_area_frac = 0.05,
  target_um_px = 4,
  n_ref = 25,
  stratify = NULL,
  seed = 1,
  progress = TRUE
)
}
\arguments{
\item{manifest}{A manifest tibble from \code{\link[=fq_manifest]{fq_manifest()}} (columns \code{slide_id},
\code{path}, plus any covariates).}

\item{analyzer}{An analyzer spec such as \code{\link[=fq_kmeans]{fq_kmeans()}} to fit on the
subsample, or an already fitted \link{fq_analyzer} to apply directly.}

\item{n}{Number of tissue sections to keep per slide, passed to \code{\link[=fq_split]{fq_split()}}.}

\item{close_um}{Morphological closing radius in microns, passed to
\code{\link[=fq_split]{fq_split()}}. It bridges alveolar airspace within a section without
bridging the gap between sections.}

\item{min_area_frac}{Minimum area of a connected component for \code{\link[=fq_split]{fq_split()}},
as a fraction of the largest. Smaller components such as debris and scan
streaks are dropped.}

\item{target_um_px}{Working resolution in microns per pixel for \code{\link[=fq_read]{fq_read()}}.
The nearest level is chosen. Keep this matched between the fit and the
scored slides so the basis and the data it scores share a resolution.}

\item{n_ref}{Total number of slides to subsample for the fit (default 25), or
\code{Inf}/\code{NULL} to use every slide. This budget bounds the read-and-split cost
of fitting. Ignored when \code{analyzer} is already fitted. Only the first
section of each reference slide is pooled, so similar sections from one
slide are not counted twice.}

\item{stratify}{Optional manifest column to balance the subsample across,
such as \code{"treatment"}, so the fit spans the range of injury. The \code{n_ref}
budget is spread as evenly as possible across the column's groups. When
there are more groups than \code{n_ref}, a random \code{n_ref} of them are used, one
slide each.}

\item{seed}{Seed for the fit's slide subsample and k-means restarts, for a
reproducible basis. Scoring is deterministic and needs no seed.}

\item{progress}{Show a progress bar that advances as each slide finishes, in
both sequential and parallel runs. \code{FALSE} suppresses it.}
}
\value{
An \link{fq_results} object. Its \verb{@scores} slot has one row per section,
with columns \code{slide_id}, \code{section} (the section label), the analyzer's
metrics from \code{\link[=fq_score]{fq_score()}}, and the manifest's covariates joined on. \verb{@fit}
is the fitted \link{fq_analyzer}, for reuse or rendering. \verb{@params} is the split
recipe (\code{n}, \code{close_um}, \code{min_area_frac}, \code{target_um_px}) and \verb{@paths} maps
each \code{slide_id} to its file. Together \verb{@fit}, \verb{@params}, and \verb{@paths} are
everything \code{\link[=fq_map]{fq_map()}} needs to redraw any section's severity map after the
run.
}
\description{
Runs the full pipeline over a manifest. It fits the analyzer once on a
representative subsample of slides, or reuses a fit you pass in, then splits
and scores every slide. It returns one row per section with the manifest's
covariates joined on. A slide that fails to read or score is skipped with a
warning rather than stopping the run. It does not produce severity maps.
Generate any you need with \code{\link[=fq_render]{fq_render()}} from the returned fit and a section.
}
\details{
Parallelism is optional and controlled by the caller. Set \code{mirai::daemons(n)}
in your session and the work for each slide runs across those daemons. With
no daemons set it runs sequentially. A parallel run needs the package
installed with \verb{R CMD INSTALL}, because each daemon loads \code{fibroquant} to
score.
}
\seealso{
\code{\link[=fq_map]{fq_map()}}
}
