% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{fq_results}
\alias{fq_results}
\title{The result of a scoring run}
\usage{
fq_results(scores = data.frame(), fit, params = list(), paths = data.frame())
}
\arguments{
\item{scores}{A tibble with one row per section: \code{slide_id}, \code{section}, the
analyzer's metrics from \code{\link[=fq_score]{fq_score()}}, and the manifest's covariates.}

\item{fit}{The fitted \link{fq_analyzer} the run scored with, for reuse or to
render maps. Typed to the \link{fq_analyzer} superclass so any analyzer fits.}

\item{params}{The split recipe that produced the scored sections, a named
list with \code{n}, \code{close_um}, \code{min_area_frac}, and \code{target_um_px}.}

\item{paths}{A two-column tibble mapping each \code{slide_id} to its \code{path}, so a
scored section can be replayed from its source file.}
}
\value{
An \code{fq_results} object.
}
\description{
The object \code{\link[=fq_run]{fq_run()}} returns: the per-section scores together with
everything needed to redraw any section's severity map after the fact. It is
a typed container over plain tibbles, so \verb{@scores} stays a \code{dplyr}-able table
for the downstream stats join while the run carries its fit and the recipe
that produced it.
}
\details{
Because the read-and-split path is deterministic, the run does not cache any
pixels. The \code{fit} plus the split \code{params} and the slide \code{paths} are the
closed set needed to replay a section and render its map. \code{\link[=fq_map]{fq_map()}} does
that replay in one call.
}
\seealso{
\code{\link[=fq_run]{fq_run()}}, \code{\link[=fq_map]{fq_map()}}
}
